apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
type: Generic
data:
  username: {{ .Values.env.DB_USER | b64enc | quote }}
  password: {{ .Values.applicationSecret.password | b64enc | quote }}
  DB_PASSWORD: {{ .Values.applicationSecret.password | b64enc | quote }}